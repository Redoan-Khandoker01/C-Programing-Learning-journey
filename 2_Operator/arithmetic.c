#include<stdio.h>
int main()
{

   //Arithmetic Operator ( + - * / % ) using for calculation
   // (% -mod) mod using to get  ভাগশেষ 

   int a = 25; 
   int b =  5 ;  
  // intiger value 

   int sum =a+b; 
   int sub = a-b;
   int div = a/b;
   int Multi= a*b;
   int mod = a%b;

    printf("1 sumation = %d \n", sum);
    printf("2 subtraction = %d\n",sub);
    printf("3 division %d\n",div);
    printf("4 Multiplication %d\n", Multi);
    printf("5 Mod = %d\n", mod);

    // thats all about Arithmetic Operator.

    // Relational Operator( >,<,>=, <= ,=, !=)
        // we used this Operator to compere 2 value 

    









    return 0;
}